# -- Main Configuration
HOSTNAME=domain.local
DOMAIN=domain.local www.domain.local

DATA_DIR=/opt/web/live
WEB_DIR=/opt/web/live/html/

# -- Caddy Configuration

# Add extra Caddy configuration here if needed
# Example: tls internal
CADDY_CONFIG_EXTRA=""
CADDY_CONFIG_EXTRA_TOOLS=""

# -- PHP Configuration
PHP_VERSION=8.4-fpm

# -- Database Configuration
MARIADB_USER=defaultdb
MARIADB_DATABASE=defaultdb
MARIADB_PASSWORD=defaultdb
MARIADB_ROOT_PASSWORD=defaultadmin

# -- Admin Tools Configuration

# [i] The `filemanager` container will spit out a password to be used for the admin interface.
# Make sure to save it in a secure place!

# Use 'admin' or 'donotstart' to enable or disable admin tools
# You can then use 'docker-compose --profile admin up -d' to start admin tools
ADMIN_TOOLS_PROFILE=donotstart 

MANAGEMENT_USER=defaultadmin
MANAGEMENT_PASSWORD=defaultadmin

# Hashed password for caddy
# Use `caddy hash-password --plaintext $MANAGEMENT_PASSWORD`
# or `https://bcrypt-generator.com/` to generate the hash
# 
# [i] Remember to save the non hashed password in a secure place.
# You will need it to login to the management interface.
MANAGEMENT_PASSWORD_HASH=[CREATE_YOUR_OWN_HASH_HERE]